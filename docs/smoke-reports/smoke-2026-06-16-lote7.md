# Smoke — 16 Jun 2026 — lote 7

**Executor:** agente (pré-flight automatizado) + pendente manual no app  
**Ambiente:** `volley-track-dev-4596c`  
**Revisão:** `0f0be21`  
**App:** `flutter run` (dev) — validação UI manual pelo time

---

## Pré-flight automatizado

| Verificação | Resultado | Detalhe |
|-------------|-----------|---------|
| Deploy `firestore.rules` | **Pass** | Compilou e publicou |
| Deploy CF críticas | **Pass** | `callMatchToCourt`, `generateCategoryBracket`, `applyLeagueRankingForMatch`, `declareMatchWalkover`, `validateMatchResult` |
| Validação rules (MCP) | **Pass** | Sem erros de sintaxe |
| Logs CF (ERROR 24h) | **Pass** | Nenhum erro em `callMatchToCourt` / `generateCategoryBracket` |
| `functions` npm test | **Pass** | 89/89 |
| Flutter smoke-gate (11 arquivos) | **Pass** | 109/109 — listing, inscrições, ops, ranking, dia D |
| Torneio `open` em dev | **Pass** | `p0pDmbBP5wJhjcLu4qUZ` ("nexaGO", DE, `waitlistEnabled: true`) |
| Ligas `open` em dev | **Não testado** | Nenhuma liga `open` encontrada — criar para smoke B |

---

## Roteiro A — Torneio avulso

**Torneio sugerido:** `p0pDmbBP5wJhjcLu4qUZ` (categoria `Masculino`, `double_elimination`)

| # | Passo | Resultado | Observação |
|---|-------|-----------|------------|
| A1 | Publicar torneio `open` → Competir | **Parcial** | Torneio já `open` no Firestore; confirmar visível na aba **Competir** (conta atleta) |
| A2 | Inscrever dupla + PIX/convite | **Pendente** | 0 inscrições no torneio de teste; executar com 2ª conta atleta |
| A3 | Gerar chave (≥2 confirmadas) + placar | **Pendente** | Após A2: validar botão desabilitado com &lt;2 confirmadas; gerar chave DE |
| A4 | Check-in → Chamar quadra | **Pendente** | Validar **Chamar** bloqueado sem check-in `present` em ambas equipes |
| A5 | Encerrar inscrições | **Pendente** | Atleta não deve iniciar nova inscrição após encerrar |

---

## Roteiro B — Liga

| # | Passo | Resultado | Observação |
|---|-------|-----------|------------|
| B1 | Liga publicada + etapa `open` | **Pendente** | Sem liga `open` em dev — publicar circuito com ≥1 etapa `defined` |
| B2 | Final → ranking duplas | **Pendente** | — |
| B3 | Atleta → aba Atletas | **Pendente** | — |
| B4 | Encerrar temporada | **Pendente** | — |

---

## Roteiro C — Dupla eliminação (opcional)

Torneio de teste já é `double_elimination` — pode rodar C1–C3 após A3.

| # | Resultado |
|---|-----------|
| C1 Republicar chave | Pendente |
| C2 WB R1 → LB | Pendente |
| C3 Grand Final preenchida | Pendente |

---

## Fail rápido (monitorar no manual)

- [ ] Draft visível em Competir  
- [ ] `isPaid` setado pelo cliente (sem CF)  
- [ ] **Chamar** sem check-in  
- [ ] Atleta altera `waitlist` na inscrição  
- [ ] Ranking liga vazio após final  
- [ ] Liga `closed` ainda em Competir  

---

## Bloqueadores / notas

1. **Smoke UI não automatizado** — checklist A/B exige duas contas no app (organizador + atleta). Pré-flight e deploy concluídos.
2. **Liga em dev** — criar/publicar liga antes do roteiro B.
3. **Testes fora do gate** — `tournament_partner_invite_home_logic_test.dart` (3 falhas) é pré-existente e fora do escopo A/B; não bloqueia deploy.

---

## Comandos usados neste lote

```bash
firebase deploy --only firestore:rules,functions:callMatchToCourt,functions:generateCategoryBracket,functions:applyLeagueRankingForMatch,functions:declareMatchWalkover,functions:validateMatchResult --project volley-track-dev-4596c

cd functions && npm test

cd nexago_app && flutter test \
  test/features/tournaments/tournament_listing_status_test.dart \
  test/features/tournaments/tournament_inscriptions_repository_test.dart \
  test/features/tournaments/tournament_detail_logic_test.dart \
  test/features/tournaments/league_ranking_logic_test.dart \
  test/features/tournaments/athlete_tournament_day_logic_test.dart \
  test/features/organizer/tournament_ops_logic_test.dart \
  test/features/organizer/tournament_create_logic_test.dart \
  test/features/organizer/category_ops_logic_test.dart \
  test/features/organizer/league_create_logic_test.dart \
  test/features/organizer/match_ops_logic_test.dart \
  test/features/organizer/match_scoring_logic_test.dart
```
